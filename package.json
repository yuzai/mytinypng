{
  "name": "mytinypng-monorepo",
  "version": "0.0.0",
  "private": true,
  "description": "A TinyPNG-quality image compression toolkit: core engine, CLI, web app, and AI skill.",
  "type": "module",
  "packageManager": "pnpm@10.28.1",
  "engines": {
    "node": ">=18"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["sharp", "esbuild"]
  },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "bench": "pnpm --filter mytinypng-core bench"
  }
}
